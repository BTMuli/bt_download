# 贡献指南

## 提交规则

本仓库所有 Git 提交都采用 Gitmoji。提交标题格式为：

```text
<emoji> <简短的中文动宾短语>
```

例如：

```text
✨ 实现 Magnet 下载支持
🐛 修复恢复任务后重复下载问题
✅ 补充路径穿越测试
📝 更新本地调用协议
⬆️ 升级 libtorrent 依赖
♻️ 重构任务状态转换
```

要求：

- emoji 必须放在标题开头，后接一个空格；使用实际 Unicode emoji，不使用 `:sparkles:` 等文本代码；
- 标题使用简洁、明确的中文动宾短语，不添加句号，不再附加 `feat:`、`fix:` 等 Conventional Commits 前缀；
- 一个提交只处理一个逻辑变更，源码、测试和对应文档应放在同一提交中；
- 标题建议不超过 72 个字符；需要说明背景或取舍时，在空行后添加正文；
- 提交前至少运行与变更相关的构建或测试，并确认 `git diff --check` 通过；
- 不提交 `out/`、IDE 配置、依赖缓存、临时状态文件或本地下载数据；
- 破坏兼容性的变更使用 `💥`，并在正文中说明迁移方式和受影响的协议版本。

常用 Gitmoji：

| Gitmoji | 用途 |
| --- | --- |
| ✨ | 新功能 |
| 🐛 | 缺陷修复 |
| ✅ | 新增或修正测试 |
| 📝 | 文档变更 |
| ♻️ | 不改变行为的重构 |
| ⚡️ | 性能优化 |
| 🔒️ | 安全或隐私修复 |
| ⬆️ | 升级依赖 |
| ⬇️ | 降级依赖 |
| 🔧 | 构建、配置或开发工具 |
| 👷 | CI/CD 变更 |
| 💥 | 破坏兼容性的变更 |
| 🔥 | 删除代码或文件 |
| 🚑️ | 紧急修复 |

若一项变更同时符合多个 Gitmoji，选择最能表达提交主要目的的一个。
